# Google Search Console 登録手順書

このサイトを **Google 検索に載せる**ための手順書です。

## なぜ必要か

現状、サイトの設定は「検索に載せてよい」状態（`robots.txt` 全許可・`sitemap.xml`・canonical・構造化データすべて設置済み）ですが、**Google はまだこのサイトを見つけていません**。

Google はページを「他サイトからのリンク」か「Search Console への登録」で発見します。このサイトは外部リンクがゼロ・Search Console 未登録のため、`sitemap.xml` を置いていてもその存在を知らせる手段がなく、読まれていません。

→ **Search Console に登録して sitemap を送信すれば、Google に直接「ここにサイトがあります」と伝えられます。** これが決定打です。

---

## 全体の流れ（岡﨑さん ⇄ 制作側の往復は1回だけ）

| # | 担当 | やること |
|---|---|---|
| 1 | **岡﨑さん** | Search Console にサイトを登録し、**確認コード**をコピーして制作側へ送る |
| 2 | **制作側** | 確認コードをサイトに埋め込んで公開（数分で反映） |
| 3 | **岡﨑さん** | Search Console に戻って「確認」ボタンを押す |
| 4 | **岡﨑さん** | sitemap を送信し、インデックス登録をリクエスト |

以降は Google 側の処理待ちです（**数日〜数週間**かかります）。

---

## STEP 1（岡﨑さん）サイトを登録して確認コードを取得

1. Google アカウントでログインした状態で **https://search.google.com/search-console** を開く
2. 「プロパティを追加」を選ぶ
3. 種類は **左の「ドメイン」ではなく、右の「URL プレフィックス」** を選ぶ
   - ⚠️ このサイトは `github.io` 上にあるため、「ドメイン」型（DNS 認証）は**使えません**。必ず「URL プレフィックス」を選んでください
4. 入力欄に、末尾のスラッシュまで含めて**そのまま**貼り付ける：

   ```
   https://okazakiit.github.io/okazaki-itc-site/
   ```

5. 「続行」を押すと**所有権の確認**画面が出るので、その中の **「HTML タグ」** を開く
6. 次のような1行が表示されるので、**行ごとコピー**して制作側（めたもる）へ送る

   ```html
   <meta name="google-site-verification" content="ここに長いランダムな文字列" />
   ```

7. **この画面は閉じずに残しておく**（STEP 3 で「確認」ボタンを押します）。閉じてしまっても、同じ手順でやり直せば同じコードが出ます

---

## STEP 2（制作側）確認コードをサイトに埋め込む

1. `index.html` の `<head>` 内（`<title>` の近く）に、受け取った meta タグを1行貼る
2. コミットして `main` に push する

   ```
   git add index.html
   git commit -m "chore(seo): Search Console 所有権確認タグを追加"
   git push
   ```

3. push 後、**2〜3分**でGitHub Pages に反映される
4. 反映確認：https://okazakiit.github.io/okazaki-itc-site/ を開き、ページのソース（Ctrl+U）に `google-site-verification` の行があればOK
5. 反映できたら岡﨑さんに「入れました」と連絡する

---

## STEP 3（岡﨑さん）所有権を確認する

1. STEP 1 で開いたままの Search Console の画面に戻る
2. **「確認」** ボタンを押す
3. 「所有権を証明しました」と出れば成功

> **うまくいかない場合**
> - 「確認できませんでした」→ サイトへの反映がまだの可能性大。**5分ほど待ってから**もう一度「確認」を押す
> - それでもダメなら、ブラウザで https://okazakiit.github.io/okazaki-itc-site/ を **Ctrl+F5**（スーパーリロード）してから再挑戦

---

## STEP 4（岡﨑さん）sitemap を送信し、インデックス登録をリクエストする

**所有権の確認が終わっただけでは、まだ検索には載りません。** 続けて次の2つをやります。

### 4-1. sitemap の送信

1. 左メニューの **「サイトマップ」** を開く
2. 「新しいサイトマップの追加」の入力欄に、**`sitemap.xml`** とだけ入力する
   - 欄の左側に `https://okazakiit.github.io/okazaki-itc-site/` が既に表示されているので、その続きを打つ形です
3. 「送信」を押す
4. ステータスが **「成功しました」** になればOK

### 4-2. インデックス登録のリクエスト（早く載せるため）

1. 画面上部の検索窓（「URL を検査」）に、次を貼って Enter：

   ```
   https://okazakiit.github.io/okazaki-itc-site/
   ```

2. 「URL が Google に登録されていません」と出るはずなので、**「インデックス登録をリクエスト」** を押す
3. 1〜2分待って「リクエストを送信しました」と出れば完了

---

## 登録できたかの確認方法

**数日〜数週間**してから、Google で次のように検索してください。

```
site:okazakiit.github.io/okazaki-itc-site/
```

サイトが表示されれば、Google に登録されています（現時点では0件です）。

Search Console の左メニュー「ページ」でも、登録済みページ数を確認できます。

---

## 検索で見つけてもらいやすくする補助策

Search Console への登録は「Google に見つけてもらう」ための手続きです。**実際に検索結果で上位に出るか**は別の要素で決まります。効果の大きい順に：

### 1. 外部サイトから1本でもリンクを張る（効果大・すぐできる）

Google は「他サイトからリンクされているか」を信頼度の指標にします。岡﨑さんが既に情報を掲載している次のページに、サイトURLを追記してもらうのが最も手軽です。

- **LinkedIn プロフィール**（ご本人で編集可能）
- **おかやま企業情報ナビ**（岡山県産業振興財団／掲載内容の更新を財団に依頼）

### 2. 独自ドメイン（中長期・任意）

`okazakiit.github.io/okazaki-itc-site/` より、会社名のドメイン（例：`okazaki-itc.co.jp`）の方が、名刺に書いたときの見栄えも検索での評価も上がります。年額1,000〜4,000円程度。**現時点では「対応しない」方針**ですが、必要になれば対応可能です。

### 3. 期待値について（正直なところ）

1ページの名刺サイトで現実的に拾われるのは、**「岡﨑ＩＴコンサルティング」「岡﨑好信」といった固有名詞での検索**が中心です。

「岡山 生産管理 コンサル」のような一般的なキーワードで上位を狙うには、**継続的な記事コンテンツの追加**が必要で、これは名刺サイトとは別の取り組みになります。

なお固有名詞で検索した場合も、現状は Yahoo!マップ・LinkedIn・おかやま企業情報ナビ・法人検索サイトが先に出てきます。公式サイトとしてそれらと並んで表示されるようになる、というのが当面のゴールです。

---

## 注意事項

- **確認用プレビューサイト（`metamol.github.io/site-preview/`）は Search Console に登録しないでください。** あちらは検索避け（noindex）を入れたデザイン確認専用で、承認後に削除します
- 所有権の確認に使った meta タグは、**消さないでください**。削除すると所有権が外れ、Search Console が使えなくなります
- Search Console の登録は**サイトの表示や見た目には一切影響しません**（meta タグは画面に出ません）

---

## 補足：制作側が先に登録することもできます

岡﨑さんの手を煩わせたくない場合、**制作側（めたもる）の Google アカウントで先に STEP 1〜4 をすべて済ませ、あとから Search Console の「設定 → ユーザーと権限」で岡﨑さんを「オーナー」として追加する**こともできます。

この場合、岡﨑さんは**招待メールを承認するだけ**で済みます。どちらの進め方がよいか、ご相談ください。
