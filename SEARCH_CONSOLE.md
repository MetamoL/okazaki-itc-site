# Google Search Console 登録手順書

このサイトを **Google 検索に載せる**ための手順書です。

Google はページを「他サイトからのリンク」か「Search Console への登録」で発見します。このサイトは外部リンクがゼロなので、**Search Console に登録して sitemap を送信する**のが決定打です（`robots.txt` 全許可・`sitemap.xml`・canonical・構造化データはすべて設置済み）。

## 進捗

- ✅ **STEP 1〜3（プロパティ登録・所有権の確認）完了** — `index.html` の `<head>` に `<meta name="google-site-verification" …>` を設置済み（**削除禁止**。外すと所有権が失われ Search Console が使えなくなります。表示や見た目には一切影響しません）
- ❓ **STEP 4（sitemap 送信・インデックス登録リクエスト）＝状態未確認** — 岡﨑さんの操作のため、制作側からは済んだかどうか分かりません。**Search Console の画面（左メニュー「サイトマップ」）でご確認ください**。未送信ならこの先の手順を実施、送信済みなら以降は Google 側の処理待ち（**数日〜数週間**）です

> 登録し直す場合の要点: プロパティの種類は**「ドメイン」ではなく「URL プレフィックス」**を選ぶ（`github.io` 上のサイトは DNS 認証が使えません）。URL は `https://okazakiit.github.io/okazaki-itc-site/` を末尾のスラッシュまでそのまま入力。所有権確認の「HTML タグ」を制作側へ送る → `index.html` の `<title>` 付近に1行貼って push（2〜3分で反映）→ Search Console で「確認」を押す。「確認できませんでした」と出たら5分待つ／`Ctrl+F5` で再読み込みしてから再挑戦。

---

## STEP 4（岡﨑さん）sitemap を送信し、インデックス登録をリクエストする

> **STEP 1〜3（プロパティ登録・所有権の確認）は完了済み**のため、この手順書は STEP 4 から始まります（番号が飛んでいるのはそのためです）。

**所有権の確認が終わっただけでは、まだ検索には載りません。** 続けて次の2つをやります。

### 4-1. sitemap の送信

1. 左メニューの **「サイトマップ」** を開く
2. 「新しいサイトマップの追加」の入力欄に **`sitemap.xml`** とだけ入力する（欄の左側に `https://okazakiit.github.io/okazaki-itc-site/` が既に表示されているので、その続きを打つ形です）
3. 「送信」を押し、ステータスが **「成功しました」** になればOK

### 4-2. インデックス登録のリクエスト（早く載せるため）

1. 画面上部の検索窓（「URL を検査」）に `https://okazakiit.github.io/okazaki-itc-site/` を貼って Enter
2. **「インデックス登録をリクエスト」** を押す
3. 1〜2分待って「リクエストを送信しました」と出れば完了

---

## 登録できたかの確認方法

**数日〜数週間**してから、Google で `site:okazakiit.github.io/okazaki-itc-site/` と検索してください。表示されれば登録されています（未登録なら0件）。Search Console の左メニュー「ページ」でも登録済みページ数を確認できます。

---

## 検索で見つけてもらいやすくする補助策

Search Console への登録は「Google に見つけてもらう」ための手続きです。**実際に検索結果で上位に出るか**は別の要素で決まります。効果の大きい順に：

1. **外部サイトから1本でもリンクを張る（効果大・すぐできる）** — Google は他サイトからのリンクを信頼度の指標にします。既に情報が載っている **LinkedIn プロフィール**（ご本人で編集可能）と **おかやま企業情報ナビ**（岡山県産業振興財団に掲載更新を依頼）にサイトURLを追記してもらうのが最も手軽です。
2. **独自ドメイン（中長期・任意）** — 会社名のドメイン（例：`okazaki-itc.co.jp`・年額1,000〜4,000円程度）の方が名刺の見栄えも検索での評価も上がります。**現時点では「対応しない」方針**ですが、必要になれば対応可能です。
3. **期待値について（正直なところ）** — 1ページの名刺サイトで現実的に拾われるのは「岡﨑ＩＴコンサルティング」「岡﨑好信」といった**固有名詞での検索**が中心です。「岡山 生産管理 コンサル」のような一般キーワードで上位を狙うには継続的な記事コンテンツの追加が必要で、名刺サイトとは別の取り組みになります。固有名詞で検索した場合も、現状は Yahoo!マップ・LinkedIn・おかやま企業情報ナビ・法人検索サイトが先に出てきます。公式サイトとしてそれらと並んで表示されるようになる、というのが当面のゴールです。

---

## 補足：制作側が先に登録することもできます

岡﨑さんの手を煩わせたくない場合、**制作側（めたもる）の Google アカウントで STEP 1〜4 をすべて済ませ、あとから「設定 → ユーザーと権限」で岡﨑さんを「オーナー」として追加する**こともできます。この場合、岡﨑さんは招待メールを承認するだけで済みます。
